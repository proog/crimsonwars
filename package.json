{
  "name": "crimsonwars",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rimraf ./_site",
    "prestart": "npm run clean",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "bootstrap": "^4.3.1",
    "rimraf": "^6.0.1"
  }
}
