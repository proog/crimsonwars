{
  "name": "crimsonwars",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rimraf ./_site",
    "prestart": "npm run clean",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.2",
    "rimraf": "^3.0.2"
  }
}
